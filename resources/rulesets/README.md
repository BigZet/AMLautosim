# Rulesets

Неизменяемые версионированные конфигурации, например:

- `game-v1.json` — лимиты и начальные ресурсы;
- `scoring-v1.json` — веса и пороги риска;
- `cards-v1.json` — каталог доступных карточек.

Ruleset активированного раунда не изменяется задним числом.
