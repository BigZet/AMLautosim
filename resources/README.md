# Versioned application resources

- `rulesets/` — immutable game/scoring configuration addressed by version;
- `fixtures/` — synthetic, PII-free examples used by development and tests.

Production participant data does not belong in the repository.
