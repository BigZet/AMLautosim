# Use the digest from the release-provenance artifact, not a mutable tag.
AML_IMAGE=ghcr.io/bigzet/amlautosim@sha256:REPLACE_WITH_VERIFIED_DIGEST
POSTGRES_DB=aml_simulator
POSTGRES_USER=aml
POSTGRES_PASSWORD=
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=
NICEGUI_STORAGE_SECRET=
UI_PORT=8080
# The production compose enforces secure cookies and a loopback-only UI port.
# Terminate HTTPS at the trusted reverse proxy before exposing this endpoint.
