# Зафиксированный выпуск 2.0.0

Основа — `38b32a7` из `codex/review-remediation-2026-09-25`, включая последние
незакоммиченные правки интерфейса из её рабочего каталога. Основной каталог
проекта обновлён до этой версии. Идентификатор окончательного состояния — Git-тег
`release-2026-09-25`.

Единственная модель — `aml-game-organizer-settings-v1`, контракт v10,
28 признаков. Веса не переобучались. Контрольные суммы выпуска записаны
в [RELEASE.json](../RELEASE.json), реестр моделей — в
[registry.json](../resources/catboost_models/registry.json).

Сохранены фоновые задания скоринга, восстановление worker, настройки организатора
и последние изменения интерфейса: несколько открытых операций, прямые кнопки
действий и таблицы SHAP без дополнительного раскрытия.

Из текущего дерева удалены прежние модели, кандидаты обучения, датасеты,
архивные отчёты, скриншоты и планы. Docker, CI и упаковщик включают только текущую
модель. Старые раунды больше не поддерживаются; версия 8 отклоняется командами
каталога, настроек и перезапуска. Рабочая БД не менялась.

Общие тесты API и worker переведены на v10. Удалены проверки снятых пакетов и
замороженных демонстраций. Нужные алгоритмам шаблоны сценариев сохранены в
`config/aml_scenario_templates.json`; пилотные тестовые входы генерируются во
временном каталоге. Миграции БД и используемые регрессионные фикстуры сохранены.
Три прежние рабочие копии Git удалены после переноса последних правок; история
коммитов сохранена. Удалены прежний локальный архив поставки и временные отчёты.

## Проверки

Проверка выполнена локально на Windows, Python 3.13.15 и PostgreSQL 16:

- Ruff, отдельный runtime-профиль Ruff, mypy и проверка совместимости зависимостей — успешно.
- Runtime/unit/ML: 656 passed, 1 skipped (создание symlink без привилегии Windows).
- API: проверены все 223 сценария. В полном прогоне прошли 216; семь ошибок
  обновляемых фикстур устранены и покрыты успешным повторным прогоном 10 тестов.
  Проверены в том числе 100 сценариев скоринга, конкуренция и восстановление worker.
- Изменённые компоненты UI: дополнительный прогон 20 unit-тестов — успешно.
- Браузеры: проверены 63 сценария Chrome, Firefox и WebKit. После исправления
  сдвига поля повторно прошли все 26 затронутых проверок: 20 Firefox/WebKit и 6 Chrome.
- Docker: сборка, миграции, readiness, восстановление отдельной копии,
  ограничение ingress, HTTP, cookie и WebSocket — успешно.
- Runtime-архив: 361 файл; два независимых запуска упаковщика дали одинаковый SHA-256
  `bb87347e7a25ce22b66b969183e794e5521171bc5bbbc9eca4c41034ab51fa0a`.

В ходе проверки стабилизирован порядок `detail_factors` в ответах API после
JSONB-сохранения. Для временной строки состояния preview зарезервировано место,
чтобы завершение пересчёта не сдвигало поле ввода на планшетной ширине в WebKit.
Веса модели и извлечение признаков не изменялись.

Исследовательский набор: 349 passed и 69 известных failures; новых ошибок нет.
Проверка `scripts.ci_test_policy research` прошла. Эти ошибки остаются в
`tests/research-baseline.json` и не считаются успешным повторным обучением модели.
Runtime inventory обновлён для единственного поддерживаемого пакета: удалены
проверки архивных пакетов и параметры v8, проверки текущего v10 сохранены.

Исходные журналы, отчёты повторных прогонов и runtime-архив находятся локально
в игнорируемом `.local-run/current-release/`. Публикация и развёртывание не выполнялись.
