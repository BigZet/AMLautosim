# Финальная проверка и очистка — 2026-09-18

Обучение завершено. Принятую модель и датасет не пересобирали и не переобучали.
Текущий пакет: `resources/catboost_models/aml-game-attribute-context-v1`.
Датасет: `E:/AMLautosim-artifacts/aml-probability-v1/game-attribute-context-v1r2-62000`.

## Проверки

- 993 уникальных теста прошли, 1 пропущен, неустранённых падений нет.
  Это сводка общего прогона и адресных повторных прогонов после исправлений,
  а не один непрерывный зелёный прогон. Проверены `tests/unit`, `tests/ml`
  и четыре набора API-проверок релизов классификатора с PostgreSQL.
- Первоначальный общий прогон остановился после 15 ошибок: 917 passed,
  14 failed, 1 error, 1 skipped. Причины: устаревшие фикстуры с запрещённым
  назначением `family_support`, ожидание закрытого до выпуска v10, старые
  предположения о дополнительных профилях и UI без автозаполнения.
- Совместимые исторические цепочки выбраны без изменения их операций и меток.
  Отклонение устаревших назначений проверяется отдельно; серверная валидация
  не ослаблена. Тесты пяти удалённых профильных конструкций проверяют явный отказ.
- UI-тесты теперь проверяют значения по умолчанию и отсутствие селектора
  при единственном варианте. Сохранение, восстановление, изменение стороны
  и отображение реквизитов продолжают проверяться.
- В Windows pytest с multiprocessing запускается из файла с main guard,
  а не через stdin. Тестовые мини-модели не меняют выпущенные веса.
- `ruff check src scripts tests migrations`: passed.
- Целостность текущего и трёх предыдущих игровых пакетов проверена.
- Оба API `/health/ready` вернули 200, БД подключены, миграции на head.
  Оба UI `/play` вернули 200. Это HTTP-проверка доступности, не новый
  браузерный плейтест.

Машиночитаемая сводка: [final-checks-2026-09-18.json](final-checks-2026-09-18.json).
Исходные JUnit и логи сохранены локально в `.local-run/classifier-release/`.
При сводке заменены старые имена семи переименованных тестовых случаев;
остальные результаты объединены по classname/name с приоритетом последнего запуска.
Два предупреждения библиотек FastAPI/Starlette о deprecated API не блокируют выпуск.

## Неизменные результаты приёмки модели

Из [отчёта выпуска](attribute-context-release-2026-09-18.md): 62 000 цепочек,
28 признаков, серая зона test 15,7963%, точное совпадение серверного и
проверочного инференса на 6 166 test-цепочках. На 5 590 стрессовых цепочках
4 ошибки выше 0,25 при согласованном пределе 20. Эти вычисления не повторялись
при очистке; хеши живой модели совпадают с принятой версией.

- package SHA256: `eb686e2fede5f010f8e87f70785d600d477e508cabb72b154013e05b4200232b`
- weights SHA256: `514b0b03e794bc54a6d74b1cfd7981dad4f415063fc3cd8e567b334084efcf35`
- context SHA256: `caaa2171afba2852c467871bf8b2a0e18631645e9e8ca6112ba7f59e5b7b3d8c`

## Очистка

Из рабочих каталогов перенесены устаревшие выгрузки, промежуточные датасеты,
модели-кандидаты и старые архивы приложения: 89 записей, 758 файлов,
11 343 169 286 байт. С диска C: перенесено 9 576 215 050 байт (8,92 ГиБ).
Для всех записей проверены наличие в архиве, число файлов, общий размер
и отсутствие по старому адресу.

Архив: `E:/AMLautosim-artifacts/archive/final-cleanup-2026-09-18`.
[Манифест перемещения](final-cleanup-2026-09-18.json) позволяет восстановить пути.
Автоматическая проверка отклонила безвозвратное удаление 50 каталогов с
сообщением `blocked by policy`; вместо удаления выполнено обратимое перемещение.

Сохранены актуальный датасет, результаты обучения, источники воспроизводимости,
фикстуры регрессионных тестов и пакеты, закреплённые за старыми раундами.
Исторические отчёты сохранены как доказательства; актуальные документы перечислены
в [индексе](README.md). Учётные данные, настройки запуска и резервные копии БД
не изменены. Финальный проход очистки LFS tmp: 0 файлов, каталог пуст.

## Сборка и доступ

Обновлённый архив исходников приложения:
`.local-run/classifier-release/aml-classifier-final-20260918.tar.gz` — 358 файлов,
SHA256 `7bc40b8978ee672a1e5a7a2f009a6237fb18bfeb6d8c79c4e9d5e936218c61dc`.
Предыдущий принятый архив сохранён как источник точных исходников при обучении.
Docker-образ не пересобирался: изменения этой проверки затронули документацию,
тесты и очистку кода исследовательских инструментов; runtime и веса не менялись.

- Игра: <http://127.0.0.1:8083/play>
- Организатор: <http://127.0.0.1:8083/admin>
- Демонстрация: <http://127.0.0.1:8082/play>

Очистка не удаляла и не мигрировала раунды или пользовательские результаты.
