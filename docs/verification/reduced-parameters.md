> Исторический отчёт проверки сокращения параметров до очистки проекта.
> Пути, команды и результаты ниже относятся к той версии, а не к текущему серверу.

# Проверка сокращённых параметров — 12 сентября 2026

Реализован контракт конфигурации 7 / каталога 4 / game-rules-v5 / risk-rules-v3.
Сумма сохранена у всех операций. Дополнительных полей: зарплата — 1,
входящий перевод — 5, перевод по карте — 4, снятие наличных — 3.
Зарплата сохраняет пустой context, снятие и перевод по карте — пустой action_details.
Модель CatBoost не обучалась; пересозданы 300 учебных примеров JSON/CSV.

## Автоматические проверки

- Полный запуск unit/API: **182 passed**, 165,12 с.
- После исправления уведомления организатора: **4 passed** (NiceGUI forms).
- После исправления индикатора превышенного лимита: **3 passed** (incoming form, включая новую регрессию). Полный набор после добавления этой регрессии повторно не запускался.
- Нагрузочная проверка: **1 passed**, 50 участников, 200 чтений состояния, медиана 858 мс, p95 1653 мс, скоринг 30 мс.
- Валидация конфигурации, контроль баланса, compileall, Ruff F/I и git diff --check успешны.
- Синтетика повторно сгенерирована и сравнена; JSON и все признаки CSV соответствуют генератору.
- Три предупреждения тестового окружения относятся к зависимостям.

Контрольные маршруты сохранили ресурсы и игровые баллы:

| Маршрут | Баланс | Энергия | Время | Риск | Игровой балл |
|---|---:|---:|---:|---:|---:|
| transfers | 8000 | 11 | 3 | 20,00 | 61,51 |
| mixed | 3000 | 3 | 0 | 25,83 | 53,38 |
| incoming_funding | 7950 | 9 | 1 | 21,00 | 58,84 |

Логи: [unit/API](reduced-parameters/tests-second.txt), [нагрузка](reduced-parameters/load.txt),
[формы](reduced-parameters/forms-final.txt), [индикатор](reduced-parameters/limit-final.txt).

## Самостоятельная HTTP-проверка FastAPI

До изменения UI поднят настоящий Uvicorn на 8002 с отдельной PostgreSQL-базой
aml_reduced_manual. Запросы отправлялись по сети через httpx, без ASGITransport и pytest.

Созданы игра и участник, выполнен вход обеими ролями. Проверены точный каталог и
порядок полей, изменение каждого оставшегося параметра всех четырёх операций,
preview, сохранение и повторное чтение. 14 запросов с удалёнными или неприменимыми
полями отклонены с ошибками полей. Лишних ключей и факторов документов нет.
Отправка, скоринг и чтение результата завершились: риск 21,00, игровой балл 58,84.

Проверено время на суммах 59 999,99 / 60 000 / 60 000,01 ₽. Дополнительно выполнены
девять полных HTTP-циклов скоринга для входящего перевода, перевода по карте и снятия:
корректировка −5 риска появляется строго от 60 000 ₽ включительно.

Результаты: [HTTP-сценарии](reduced-parameters/http-report.json),
[границы суммы](reduced-parameters/http-boundaries.json).

## Самостоятельная проверка в браузере

Настоящий Chrome с NiceGUI на 8083, подключённым к проверенному API и отдельной БД:
настройка и запуск организатором, вход участником, все четыре операции, изменение
каждого поля, автосохранение, перезагрузка и сверка с API. Проверены повторное
редактирование, копирование, перестановка и удаление; выполнены отправка, скоринг
организатором и просмотр вкладок результата участником.

Браузерное прохождение выполнено через Playwright с установленным Chrome,
отдельно от компонентных тестов; затем интерфейс дополнительно осмотрен в обычном
окне Chrome через управление мышью. На ширинах 1440, 768 и 390 px нет горизонтальной
прокрутки; осмотрены переносы, поля и кнопки. JavaScript pageerror отсутствуют;
при заключительном осмотре DevTools — 0 сообщений консоли. Логи API/UI проверены.

Исправлены две обнаруженные проблемы: уведомление после пересоздания формы
организатора и зелёная отметка при превышении лимита ночных операций. Уведомление
повторно проверено в полном прохождении; индикатор — отдельным тестом и визуально
в Chrome (3/2 ночных операций, незаполненный индикатор, отправка недоступна).

Скриншоты форм: [1440 px](reduced-parameters/participant-1440.png),
[768 px](reduced-parameters/participant-768.png), [390 px](reduced-parameters/participant-390.png).
[Результат](reduced-parameters/result.png). Снимки форм сделаны до заключительного
исправления индикатора лимита; состав и расположение полей после этого не менялись.
[Журнал прохождения](reduced-parameters/browser-report.json).

Проверена открытая вкладка участника при смене игры: формы сменились ожиданием,
после запуска новой игры показан пустой сценарий. Четыре старых шага не восстановились.

## Рабочее окружение

Рабочие API/UI остановлены, bootstrap выполнен с явным --reset-game в базе
aml_simulator_v2, сервисы снова запущены на 8000/8080. Таблицы PostgreSQL не менялись.
Сохранены 2 пользователя и 25 существовавших сессий; прежняя API-сессия проверена
после обновления. Сценарии и результаты сброшены. Новая игра № 8 оставлена в draft.
Обычный seed игру автоматически не удаляет.

Проверены рабочий каталог, доступность обоих сервисов и панель организатора
в Chrome: точные наборы полей и ожидание начала игры. Старая рабочая вкладка после
перезапуска потребовала вход, после входа отобразила новую игру.
[Результат внедрения](reduced-parameters/deployment.json).
