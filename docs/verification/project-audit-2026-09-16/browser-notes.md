# Browser smoke, 2026-09-16

Chrome, отдельная вкладка аудита, приложение `http://127.0.0.1:8080`.

1. `/play/login`: заголовок «AML Практикум», поля Email/Пароль, кнопка «Войти», переходы на регистрацию и вход организатора. Русский текст читаемый, форма без видимых наложений в desktop viewport.
2. Нажатие «Войти» без данных: в форме появляется «Укажите email и пароль.», страница не падает.
3. Переход «Регистрация»: поле имени, email, пароль и повтор пароля, подсказка 10–128 символов.
4. Переход «Вход организатора»: отдельный заголовок, поля email/пароль, обратная ссылка участнику. Видимых ошибок отображения нет.

Проверка выполнена через CUA, AX tree и визуальный снимок. Снимки показаны в сессии, отдельные PNG в этот каталог не экспортировались. Учётные записи через браузер не создавались, вход существующими учётными данными не выполнялся. Полный игровой путь и мобильная адаптация этой smoke-проверкой не подтверждены; функциональные пути покрыты отдельным автоматическим API/NiceGUI-прогоном.

## Дополнительная проверка Docker-стенда

Адрес `http://127.0.0.1:58480`, отдельная новая БД. Тестовый участник создан API-сценарием `docker_acceptance.py`; его данные не относятся к реальным людям или рабочей игре.

1. Вход тестовым участником через настоящую форму Chrome прошёл; открылась завершённая игра.
2. Показаны итог **47,24**, место **1**, риск **45,30**, сбережённые ресурсы **33,39**, категория «Подозрительный сценарий».
3. Показаны положительные и отрицательные SHAP-факторы, их значения и disclaimer. В desktop viewport текст читаемый, наложений на видимой части экрана нет.
4. Reload сохранил вход. После начальной загрузки/poll вернулись тот же участник, результат и SHAP.
5. Вкладка «Рейтинг» показывает одну строку «Контейнерный аудит (вы)», место 1 и числовые значения, совпадающие с итогом.

Готовый сценарий собран и отправлен через API, не мышью в редакторе. Ручная проверка всего процесса сборки и мобильной адаптации остаётся за пределами этого браузерного smoke.
