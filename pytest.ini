[pytest]
testpaths = tests
addopts = --strict-markers --import-mode=importlib
markers =
    runtime: supported runtime, API, UI and inference contracts
    research: offline authoring, dataset generation, training and evaluation
    load: dedicated controller/WebSocket load workloads
    browser: real browser end-to-end and responsive layout checks
